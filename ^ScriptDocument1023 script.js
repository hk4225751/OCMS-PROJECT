const renderDailyContent = () => {
    const container = document.getElementById('daily-content-container');
    if (!container) return; // Guard clause to prevent the error
    
    container.innerHTML = '';
    // ... rest of the function
}